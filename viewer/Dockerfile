FROM node:20-bookworm-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
  libcairo2 \
  libpango-1.0-0 \
  libjpeg62-turbo \
  libgif7 \
  librsvg2-2 \
  && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY viewer/package.json /app/package.json
RUN npm install --omit=dev

COPY viewer/index.js /app/index.js

EXPOSE 3000

CMD ["node", "index.js"]
